import { AddressUpdateRequest } from './address-update-request';

describe('AddressUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new AddressUpdateRequest()).toBeTruthy();
  });
});
