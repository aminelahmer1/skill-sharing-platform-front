import { UserProfileUpdateRequest } from './user-profile-update-request';

describe('UserProfileUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new UserProfileUpdateRequest()).toBeTruthy();
  });
});
